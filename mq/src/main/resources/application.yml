server:
  port: 7000

spring:
  activemq:
    broker-url: tcp://192.168.173.130:61616
    user: admin
    password: admin
  jms:
    pub-sub-domain: true # false = Queue true = Topic

#自己定义队列名称
myqueue: boot-activemq-queue
mytopic: boot-activemq-topic
